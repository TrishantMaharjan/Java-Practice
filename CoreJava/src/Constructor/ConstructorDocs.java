package Constructor;

public class ConstructorDocs {
	/*
	 * ========== Constructor ==========
	 * - Used to create object in java
	 * - Special type of method which has the same name as class name is known as constructor
	 * 
	 * Types:-
	 * 	1) Default constructor
	 * 		- Used to create object and set default values to it
	 * 	2) Parameterized constructor
	 * 		- Used to create object and set dynamic values to it
	 * 		- Syntax:
	 * 			class_name(){
	 * 				//set dynamic values to it
	 * 			}
	 */
}
